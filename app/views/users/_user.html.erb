<li>
  <%= link_to user.name, user %>
	<% if current_user.admin? %>
		<% if !user.admin? || current_user == user %>
    	| <%= link_to "編集", edit_user_path(user) %>
    	| <%= link_to "削除", user, method: :delete, data: { confirm: "ユーザを削除しますか？" } %>
		<% end %>
  <% end %>
</li>
